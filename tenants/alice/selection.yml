# This file defines a user's service selection.
tenant_domain: "alice.homelab.local"
deployment_target: "proxmox-main" # <-- This is the link!
deployment_runtime: "docker"

services:
  nextcloud:
    enabled: true
    host: "cloud.alice.homelab.local"
  # ... other services